{
  "name": "local-agentic-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Edge-Native Multi-Agent Prompt Orchestrator — Chrome Extension (MV3)",
  "type": "module",
  "scripts": {
    "build": "vite build && cp manifest.json dist/manifest.json",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "vite": "^6.2.0"
  }
}
