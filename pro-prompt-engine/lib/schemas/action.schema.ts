/**
 * Action schema — stub.
 *
 * [Phase 3] fills this in with the full verb vocabulary and the Zod schema
 * that drives both z.toJSONSchema() (constrained decoding) and gate
 * validation, per architecture.md §3.7.14. Phase 1 creates only the type
 * this phase's own code needs to reference (RunRecord.plan, sitePolicy
 * capabilities) so those files do not import from a file that does not
 * exist yet.
 */

/** [Phase 3: replaced by the real verb union — perception + interaction] */
export type Verb = never;
