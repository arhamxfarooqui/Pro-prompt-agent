/**
 * Snapshot schema — stub.
 *
 * [Phase 2] fills this in with the perception snapshot shape (DOM digest,
 * element index, viewport info) and its Zod schema. Nothing in Phase 1
 * references this yet; it is created so the file tree matches
 * architecture.md §3.6 from the first commit.
 */

/** [Phase 2: replaced by the real perception snapshot shape] */
export interface Snapshot {
  readonly __phase2Stub: true;
}
